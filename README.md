# ParsingGoldApple
# GoldApple Perfume Parser

Этот проект представляет собой парсер парфюмерии с сайта
**GoldApple.ru**, созданный с использованием Python и Selenium.

## 📌 Возможности

-   Скроллинг каталога товаров (полностью или ограниченное количество
    скроллов)
-   Парсинг карточек товаров:
    -   бренд
    -   название
    -   цена
    -   рейтинг
    -   описание
    -   инструкция применения
    -   страна происхождения
    -   ссылка
-   Сохранение данных:
    -   JSON --- `goldapple_perfumes.json`
    -   CSV --- `goldapple_perfumes.csv`

## 🚀 Запуск

``` bash
python main.py
```

После запуска будет предложено указать количество скроллов:

    Сколько скроллов выполнять? (Enter для полного скролла):

## 📁 Структура проекта

    project/
    ├── main.py
    ├── README.md
    ├── requirements.txt
    ├── src
    │   ├── config.py
    │   ├── models.py
    │   ├── parser.py
    │   └── utils.py

## ⚙️ Основные файлы

### `parser.py`

Содержит класс `PerfumeParser`, который: - открывает страницу -
закрывает модальные окна - выполняет скроллинг - парсит список товаров -
открывает каждую внутреннюю карточку - извлекает описание, инструкцию и
страну происхождения

### `models.py`

Модель `Perfume` с методом `to_dict()` для сериализации в JSON/CSV.

### `utils.py`

-   `scroll_to_bottom` --- скроллинг страницы
-   `clean_html_text` --- очистка HTML
-   `save_to_json`
-   `save_to_csv`

### `config.py`

Содержит: - `USER_AGENT` - `START_URL` - `BASE_URL` - `SCROLL_PAUSE`


